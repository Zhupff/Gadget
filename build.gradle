buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath BUILD_GRADLE
        classpath KOTLIN_GRADLE
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}