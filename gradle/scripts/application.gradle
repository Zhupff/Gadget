apply plugin: "com.android.application"
apply from: rootProject.file("gradle/scripts/module-template.gradle")
apply plugin: skinresource.SRPlugin

android {
    defaultConfig {
        applicationId "the.gadget"
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
}