<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="the.gadget.theme.Colour"/>
        <variable name="user" type="the.gadget.user.User" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        themeBackgroundColor="@{Colour.Background}"
        tools:background="@color/themeBackground">

        <the.gadget.weight.common.CommonConstraintLayout
            android:id="@+id/top_layout"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:fitStatusBar="true"
            themeBackgroundColor="@{Colour.Surface}"
            tools:background="@color/themeSurface">

            <the.gadget.weight.common.CommonImageView
                android:id="@+id/iv_avatar"
                android:layout_width="64dp"
                android:layout_height="64dp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:allCornerRadius="@dimen/cornerRadiusMax"
                android:layout_marginStart="@dimen/marginMax"
                android:layout_marginTop="@dimen/marginMax"
                android:layout_marginBottom="@dimen/marginMax"
                foregroundBitmap="@{user.avatar}"
                themeBackgroundColor="@{Colour.PrimaryContainer}"
                tools:background="@color/themePrimaryContainer"/>

            <TextView
                android:id="@+id/tv_nickname"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintStart_toEndOf="@id/iv_avatar"
                app:layout_constraintTop_toTopOf="@id/iv_avatar"
                app:layout_constraintBottom_toTopOf="@id/tv_uid"
                android:layout_marginStart="@dimen/marginMax"
                android:text="@{user.nickname}"
                android:textSize="@dimen/textSizeMax"
                android:textStyle="bold"
                themeTextColor="@{Colour.OnBackground}"
                tools:textColor="@color/themeOnBackground"/>

            <TextView
                android:id="@+id/tv_uid"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintStart_toEndOf="@id/iv_avatar"
                app:layout_constraintBottom_toBottomOf="@id/iv_avatar"
                android:layout_marginStart="@dimen/marginMax"
                android:text="@{user.uid}"
                android:textSize="@dimen/textSizeMid"
                themeTextColor="@{Colour.OnBackground}"
                tools:textColor="@color/themeOnBackground"/>

        </the.gadget.weight.common.CommonConstraintLayout>

        <the.gadget.weight.common.CommonFrameLayout
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/top_layout"
            app:layout_constraintBottom_toBottomOf="parent"
            app:fitNavigationBar="true">

            <androidx.core.widget.NestedScrollView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:overScrollMode="never"
                android:requiresFadingEdge="vertical"
                android:orientation="vertical">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:paddingTop="@dimen/paddingMax"
                    android:paddingBottom="@dimen/paddingMax">

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:paddingStart="@dimen/paddingMax"
                        android:paddingEnd="@dimen/paddingMax"
                        android:orientation="vertical"
                        app:allCornerRadius="@dimen/cornerRadiusMax"
                        themeBackgroundColor="@{Colour.Surface}"
                        tools:background="@color/themeSurface"
                        tools:layout_height="200dp">
                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="@dimen/marginMin"
                            android:layout_marginBottom="@dimen/marginMin"
                            android:text="应用"
                            android:textSize="@dimen/textSizeMid"
                            themeTextColor="@{Colour.OnBackground}"
                            tools:textColor="@color/themeOnBackground"/>
                        <View
                            android:layout_width="match_parent"
                            android:layout_height="0.5dp"
                            themeBackgroundColor="@{Colour.Outline}"
                            tools:background="@color/themeOutline"/>
                        <androidx.recyclerview.widget.RecyclerView
                            android:id="@+id/rv_app_list"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="@dimen/marginMid"
                            android:layout_marginBottom="@dimen/marginMid"
                            android:overScrollMode="never"/>
                    </LinearLayout>

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:paddingStart="@dimen/paddingMax"
                        android:paddingEnd="@dimen/paddingMax"
                        android:layout_marginTop="@dimen/marginMax"
                        android:orientation="vertical"
                        themeBackgroundColor="@{Colour.Surface}"
                        tools:background="@color/themeSurface"
                        tools:layout_height="200dp">
                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="@dimen/marginMin"
                            android:layout_marginBottom="@dimen/marginMin"
                            android:text="设置"
                            android:textSize="@dimen/textSizeMid"
                            themeTextColor="@{Colour.OnBackground}"
                            tools:textColor="@color/themeOnBackground"/>
                        <View
                            android:layout_width="match_parent"
                            android:layout_height="0.5dp"
                            themeBackgroundColor="@{Colour.Outline}"
                            tools:background="@color/themeOutline"/>
                        <androidx.recyclerview.widget.RecyclerView
                            android:id="@+id/rv_option_list"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="@dimen/marginMid"
                            android:layout_marginBottom="@dimen/marginMid"
                            android:overScrollMode="never"/>
                    </LinearLayout>

                </LinearLayout>

            </androidx.core.widget.NestedScrollView>

        </the.gadget.weight.common.CommonFrameLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>