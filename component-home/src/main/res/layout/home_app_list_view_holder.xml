<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="the.gadget.theme.Colour"/>
        <variable
            name="app"
            type="the.gadget.component.home.HomeApp" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/paddingMin">

        <the.gadget.weight.common.CommonImageView
            android:id="@+id/iv_app_icon"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:allCornerRadius="@dimen/cornerRadiusMid"
            foregroundDrawableRes="@{app.icon}"
            isSelected="@{app.selected}"
            themeForegroundTint="@{app.selected ? Colour.Primary : Colour.Outline}"
            tools:src="@drawable/home_app_icon"
            tools:tint="@color/themePrimaryContainer"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="@id/iv_app_icon"
            app:layout_constraintEnd_toEndOf="@id/iv_app_icon"
            app:layout_constraintTop_toBottomOf="@id/iv_app_icon"
            android:gravity="center"
            android:includeFontPadding="false"
            android:textSize="@dimen/textSizeMin"
            android:text="@{app.name}"
            isSelected="@{app.selected}"
            themeTextColor="@{app.selected ? Colour.Primary : Colour.Outline}"
            tools:textColor="@color/themePrimaryContainer"
            tools:text="应用"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>